<%= form_with(model: additional_information) do |form| %>
  <% if additional_information.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(additional_information.errors.count, "error") %> prohibited this additional_information from being saved:</h2>

      <ul>
        <% additional_information.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :information, style: "display: block" %>
    <%= form.text_area :information %>
  </div>

  <div>
    <%= form.label :faithful_id, style: "display: block" %>
    <%= form.text_field :faithful_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
